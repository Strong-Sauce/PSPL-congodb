export const environment = {
  production: true,
  // Empty string means API calls use the same origin (frontend and backend served together)
  // e.g. /api/auth/login instead of http://something:8080/api/auth/login
  apiBaseUrl: '/api'
};
